import React from 'react'
import ReactDOM from 'react-dom'
import Application from './App'

//like a main function in most languages; runs the whole program
ReactDOM.render(<Application />, document.getElementById('root'))